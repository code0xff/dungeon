node_modules/
dist/
.DS_Store

# Mixamo FBX sources. Large, and Mixamo forbids redistributing them.
# npm run optimize-assets bakes raw/ into assets/**/*.glb; delete raw/ afterwards.
raw/

# assets/**/*.glb IS committed — the game needs it to run from a fresh clone.
