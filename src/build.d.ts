/**
 * Injected by `define` in vite.config.ts: a short hash of everything under
 * assets/. It is what makes a changed model or texture reach a returning player
 * — see the note on caching at the top of assets/sw.js.
 */
declare const __ASSET_VERSION__: string;
